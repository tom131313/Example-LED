package frc.robot.subsystems;

public class Persistance {
    // subsystem keeps doing what last command was with that command still running
    // natural consequence of the period write later storage
}
